import 'dart:typed_data';

class PdfExportService {
  Future<Uint8List> generatePdf(List<String> pages) async {
    // Integração com pacote pdf será conectada nesta camada.
    // Mantém o fluxo preparado para gerar o arquivo final.
    return Uint8List.fromList([]);
  }
}
