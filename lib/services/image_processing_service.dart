class ImageProcessingService {
  Future<String> enhanceDocument(String imagePath) async {
    // Pipeline preparado para:
    // - melhorar contraste
    // - corrigir rotação
    // - preparar imagem para impressão
    return imagePath;
  }

  Future<String> resizeReceipt(String imagePath) async {
    // Ajuste proporcional para comprovante 6x6cm no PDF A4
    return imagePath;
  }
}
