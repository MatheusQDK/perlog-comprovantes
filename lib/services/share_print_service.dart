class SharePrintService {
  Future<void> sharePdf(String path) async {
    // Integração preparada para compartilhar PDF.
    // Compatível com WhatsApp, e-mail e impressão do sistema.
  }

  Future<void> printPdf(String path) async {
    // Integração preparada para impressoras do dispositivo.
  }
}
