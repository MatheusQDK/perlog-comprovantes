class ReceiptCropService {
  /// Prepara a imagem do comprovante para impressão 6x6 cm.
  /// A implementação final de recorte usará processamento nativo de imagem.
  Future<String> prepareReceipt(String imagePath) async {
    return imagePath;
  }
}
