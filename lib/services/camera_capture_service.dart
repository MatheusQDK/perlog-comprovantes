import 'dart:typed_data';

class CameraCaptureService {
  Future<Uint8List?> captureReceipt() async {
    // Integração da câmera será conectada ao plugin camera.
    // Retorna a imagem capturada do comprovante.
    return null;
  }
}
